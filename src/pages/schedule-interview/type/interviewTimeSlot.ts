export interface InterviewTimeSlot {
  start: string
  end: string
  timeslotId: string
}

export interface SelectedDateAndTime {
  selectedDate: string
  selectedTime: string
  timeslotId: string
}

export interface TimeSlot {
  slot: string
  timeslotId: string
}
